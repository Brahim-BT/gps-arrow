## Version catalogue.
##
## These versions were current and mutually compatible in August 2026.
## Android Studio will flag newer ones — see README "Upgrading" before you take them,
## especially the AGP 8.x -> 9.x jump, which is a breaking DSL change.

[versions]
agp = "8.13.0"
kotlin = "2.2.10"
composeBom = "2025.10.00"

coreKtx = "1.15.0"
activityCompose = "1.10.1"
lifecycle = "2.9.2"
coroutines = "1.10.2"

junit = "4.13.2"

# --- v1 (maps) -------------------------------------------------------------
# MapLibre >= 11.5.0 is required for 16 KB page-size support (Play mandate),
# and >= 11.7.0 for the pmtiles:// protocol used by the offline region design.
# 11.13.5: latest stable. >= 11.5.0 is required for 16 KB page support (Play
# mandate) and >= 11.7.0 for the pmtiles:// protocol. The 11.13 line has five
# patch releases behind it, which is why it is preferred over the newer minor.
maplibre = "11.13.5"

# --- v2 (routing) ----------------------------------------------------------
brouter = "1.7.8"

[libraries]
androidx-core-ktx = { group = "androidx.core", name = "core-ktx", version.ref = "coreKtx" }
androidx-activity-compose = { group = "androidx.activity", name = "activity-compose", version.ref = "activityCompose" }
androidx-lifecycle-runtime-ktx = { group = "androidx.lifecycle", name = "lifecycle-runtime-ktx", version.ref = "lifecycle" }
androidx-lifecycle-runtime-compose = { group = "androidx.lifecycle", name = "lifecycle-runtime-compose", version.ref = "lifecycle" }
androidx-lifecycle-viewmodel-compose = { group = "androidx.lifecycle", name = "lifecycle-viewmodel-compose", version.ref = "lifecycle" }

androidx-compose-bom = { group = "androidx.compose", name = "compose-bom", version.ref = "composeBom" }
androidx-compose-ui = { group = "androidx.compose.ui", name = "ui" }
androidx-compose-ui-graphics = { group = "androidx.compose.ui", name = "ui-graphics" }
androidx-compose-ui-tooling = { group = "androidx.compose.ui", name = "ui-tooling" }
androidx-compose-ui-tooling-preview = { group = "androidx.compose.ui", name = "ui-tooling-preview" }
androidx-compose-material3 = { group = "androidx.compose.material3", name = "material3" }
androidx-compose-material-icons-extended = { group = "androidx.compose.material", name = "material-icons-extended" }

kotlinx-coroutines-android = { group = "org.jetbrains.kotlinx", name = "kotlinx-coroutines-android", version.ref = "coroutines" }

junit = { group = "junit", name = "junit", version.ref = "junit" }

# v1 / v2 — not wired into any module yet.
maplibre-android-sdk = { group = "org.maplibre.gl", name = "android-sdk", version.ref = "maplibre" }
brouter-core = { group = "org.btools", name = "brouter-core", version.ref = "brouter" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
kotlin-jvm = { id = "org.jetbrains.kotlin.jvm", version.ref = "kotlin" }
kotlin-compose = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
