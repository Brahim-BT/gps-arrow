<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      INTERNET arrives with the v1 map download and nothing else. It is used by exactly one
      class, RegionDownloader, to fetch region files the user explicitly asks for. The arrow,
      the compass, the destination store and the declination model all still work with the radio
      off, and no code path on the navigation side opens a socket.

      (Until v1 this manifest carried a comment saying a v0 build is provably offline. That was
      true and is worth not quietly losing: the guarantee is now "the arrow is offline", which is
      narrower, and it is enforced by :maps sitting downstream with nothing on the arrow path
      depending on it — see BUILD_PLAN.md 6.4.)
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        android:minSdkVersion="33" />

    <!--
      ACCESS_BACKGROUND_LOCATION is intentionally NOT requested. It triggers Play's background
      location review and buys nothing: the service is always started from a visible activity.
    -->

    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.sensor.compass"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.GpsArrow">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.GpsArrow">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              geo: is how every other offline mapping app hands a point across. Accepting it
              is most of "share a destination to GPS Arrow" for free.
            -->
            <intent-filter android:label="@string/navigate_here">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="geo" />
            </intent-filter>

            <!-- Plain text share: coordinates, plus codes, MGRS, map URLs. -->
            <intent-filter android:label="@string/navigate_here">
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.NavigationService"
            android:exported="false"
            android:foregroundServiceType="location" />
    </application>
</manifest>
